<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'CSR Volunteering Platform' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="/css/dashboard.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark" style="background: var(--primary-gradient); box-shadow: var(--shadow-medium);">
        <div class="container-fluid">
            <a class="navbar-brand fw-bold" href="/" style="font-size: 1.5rem;">
                <i class="fas fa-hands-helping me-2"></i>
                CSR Volunteering
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <% if (user) { %>
                        <li class="nav-item">
                            <a class="nav-link fw-semibold" href="/<%= user.userType %>/dashboard">
                                <i class="fas fa-home me-1"></i>Dashboard
                            </a>
                        </li>
                        
                        <% if (user.userType === 'useradmin') { %>
                            <li class="nav-item">
                                <a class="nav-link fw-semibold" href="/useradmin/dashboard">
                                    <i class="fas fa-users me-1"></i>User Management
                                </a>
                            </li>
                        <% } else if (user.userType === 'platformmanager') { %>
                            <li class="nav-item">
                                <a class="nav-link fw-semibold" href="/platformmanager/dashboard">
                                    <i class="fas fa-tags me-1"></i>Categories & Reports
                                </a>
                            </li>
                        <% } else if (user.userType === 'csrrepresentative') { %>
                            <li class="nav-item">
                                <a class="nav-link fw-semibold" href="/csrrepresentative/dashboard">
                                    <i class="fas fa-search me-1"></i>Opportunities
                                </a>
                            </li>
                        <% } else if (user.userType === 'personinneed') { %>
                            <li class="nav-item">
                                <a class="nav-link fw-semibold" href="/personinneed/dashboard">
                                    <i class="fas fa-list me-1"></i>My Requests
                                </a>
                            </li>
                        <% } %>
                    <% } %>
                </ul>
                
                <ul class="navbar-nav">
                    <% if (user) { %>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle fw-semibold user-dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="fas fa-user-circle me-1"></i>
                                <%= user.firstName || user.name || 'User' %>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end user-dropdown-menu">
                                <li><a class="dropdown-item fw-semibold" href="/<%= user.userType %>/dashboard">
                                    <i class="fas fa-home me-2"></i>Dashboard
                                </a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item fw-semibold text-danger" href="/auth/logout">
                                    <i class="fas fa-sign-out-alt me-2"></i>Logout
                                </a></li>
                            </ul>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link fw-semibold" href="/auth/login" style="border-radius: 25px; padding: 0.5rem 1rem; background: rgba(255, 255, 255, 0.1);">
                                <i class="fas fa-sign-in-alt me-1"></i>Login
                            </a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>