<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Opportunity Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/styles.css" rel="stylesheet">
</head>
<body>
    <%- include('../partials/header', { title: 'Opportunity Details', user: user }) %>
    
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h2><i class="fas fa-eye"></i> Opportunity Details</h2>
                    <a href="/csrrepresentative/dashboard" class="btn btn-outline-secondary">
                        <i class="fas fa-arrow-left"></i> Back to Dashboard
                    </a>
                </div>
                
                <% if (error) { %>
                    <div class="alert alert-danger" role="alert">
                        <i class="fas fa-exclamation-triangle"></i> <%= error %>
                    </div>
                <% } %>
                
                <% if (success) { %>
                    <div class="alert alert-success" role="alert">
                        <i class="fas fa-check-circle"></i> <%= success %>
                    </div>
                <% } %>
                
                <% if (opportunity) { %>
                    <div class="card">
                        <div class="card-header">
                            <h4 class="mb-0"><%= opportunity.title %></h4>
                        </div>
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-8">
                                    <h5>Description</h5>
                                    <p class="text-muted"><%= opportunity.description %></p>
                                    
                                    <h5>Contact Information</h5>
                                    <p class="text-muted">Created by: <%= opportunity.createdByName %></p>
                                </div>
                                <div class="col-md-4">
                                    <div class="card bg-light">
                                        <div class="card-body">
                                            <h6>Details</h6>
                                            <p><strong>Category:</strong> 
                                                <span class="badge bg-primary"><%= opportunity.categoryName %></span>
                                            </p>
                                            <p><strong>Urgency:</strong> 
                                                <% if (opportunity.urgency === 'critical') { %>
                                                    <span class="badge bg-danger">Critical</span>
                                                <% } else if (opportunity.urgency === 'high') { %>
                                                    <span class="badge bg-warning">High</span>
                                                <% } else if (opportunity.urgency === 'medium') { %>
                                                    <span class="badge bg-info">Medium</span>
                                                <% } else { %>
                                                    <span class="badge bg-secondary">Low</span>
                                                <% } %>
                                            </p>
                                            <p><strong>Status:</strong> 
                                                <% if (opportunity.status === 'approved') { %>
                                                    <span class="badge bg-success">Approved</span>
                                                <% } else if (opportunity.status === 'pending') { %>
                                                    <span class="badge bg-warning">Pending</span>
                                                <% } else if (opportunity.status === 'rejected') { %>
                                                    <span class="badge bg-danger">Rejected</span>
                                                <% } else { %>
                                                    <span class="badge bg-info">Completed</span>
                                                <% } %>
                                            </p>
                                            <p><strong>Views:</strong> <%= opportunity.viewCount || 0 %></p>
                                            <p><strong>In Shortlists:</strong> <%= opportunity.shortlistCount || 0 %></p>
                                            <p><strong>Created:</strong> <%= new Date(opportunity.createdAt).toLocaleDateString() %></p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="mt-4">
                                <form action="/bce/csrrepresentative_savetoshortlist/saveToShortlist" method="POST" class="d-inline">
                                    <input type="hidden" name="userId" value="<%= user.id %>">
                                    <input type="hidden" name="opportunityId" value="<%= opportunity.id %>">
                                    <button type="submit" class="btn btn-warning me-2">
                                        <i class="fas fa-star"></i> Add to Shortlist
                                    </button>
                                </form>
                                
                                <a href="/csrrepresentative/search-requests" class="btn btn-primary">
                                    <i class="fas fa-search"></i> Search More
                                </a>
                            </div>
                        </div>
                    </div>
                <% } else { %>
                    <div class="card">
                        <div class="card-body text-center py-5">
                            <i class="fas fa-exclamation-triangle fa-3x text-warning mb-3"></i>
                            <h5>Opportunity not found</h5>
                            <p class="text-muted">The opportunity you're looking for doesn't exist or has been removed.</p>
                            <a href="/csrrepresentative/dashboard" class="btn btn-primary">
                                <i class="fas fa-home"></i> Back to Dashboard
                            </a>
                        </div>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
    
    <%- include('../partials/footer') %>
</body>
</html>
