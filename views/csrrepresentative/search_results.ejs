<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/styles.css" rel="stylesheet">
</head>
<body>
    <%- include('../partials/header', { title: 'Search Results', user: user }) %>
    
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <h2><i class="fas fa-search"></i> Search Results</h2>
                <p>Found <%= opportunities ? opportunities.length : 0 %> opportunities</p>
                
                <% if (error) { %>
                    <div class="alert alert-danger" role="alert">
                        <i class="fas fa-exclamation-triangle"></i> <%= error %>
                    </div>
                <% } %>
                
                <% if (success) { %>
                    <div class="alert alert-success" role="alert">
                        <i class="fas fa-check-circle"></i> <%= success %>
                    </div>
                <% } %>
                
                <div class="card">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h5 class="mb-0"><i class="fas fa-list"></i> Opportunities</h5>
                        <a href="/csrrepresentative/dashboard" class="btn btn-outline-secondary">
                            <i class="fas fa-arrow-left"></i> Back to Dashboard
                        </a>
                    </div>
                    <div class="card-body">
                        <% if (opportunities && opportunities.length > 0) { %>
                            <div class="table-responsive">
                                <table class="table table-striped">
                                    <thead>
                                        <tr>
                                            <th>Title</th>
                                            <th>Category</th>
                                            <th>Urgency</th>
                                            <th>Status</th>
                                            <th>Views</th>
                                            <th>Shortlist</th>
                                            <th>Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% opportunities.forEach(opportunity => { %>
                                            <tr>
                                                <td><%= opportunity.title %></td>
                                                <td><span class="badge bg-primary"><%= opportunity.categoryName %></span></td>
                                                <td>
                                                    <% if (opportunity.urgency === 'critical') { %>
                                                        <span class="badge bg-danger">Critical</span>
                                                    <% } else if (opportunity.urgency === 'high') { %>
                                                        <span class="badge bg-warning">High</span>
                                                    <% } else if (opportunity.urgency === 'medium') { %>
                                                        <span class="badge bg-info">Medium</span>
                                                    <% } else { %>
                                                        <span class="badge bg-secondary">Low</span>
                                                    <% } %>
                                                </td>
                                                <td>
                                                    <% if (opportunity.status === 'approved') { %>
                                                        <span class="badge bg-success">Approved</span>
                                                    <% } else if (opportunity.status === 'pending') { %>
                                                        <span class="badge bg-warning">Pending</span>
                                                    <% } else if (opportunity.status === 'rejected') { %>
                                                        <span class="badge bg-danger">Rejected</span>
                                                    <% } else { %>
                                                        <span class="badge bg-info">Completed</span>
                                                    <% } %>
                                                </td>
                                                <td><%= opportunity.viewCount || 0 %></td>
                                                <td><%= opportunity.shortlistCount || 0 %></td>
                                                <td>
                                                    <form action="/bce/csrrepresentative_viewopportunity/viewOpportunity" method="POST" style="display: inline;">
                                                        <input type="hidden" name="userId" value="<%= user.id %>">
                                                        <input type="hidden" name="opportunityId" value="<%= opportunity.id %>">
                                                        <button type="submit" class="btn btn-sm btn-outline-primary me-1">
                                                            <i class="fas fa-eye"></i> View
                                                        </button>
                                                    </form>
                                                    <form action="/bce/csrrepresentative_savetoshortlist/saveToShortlist" method="POST" style="display: inline;">
                                                        <input type="hidden" name="userId" value="<%= user.id %>">
                                                        <input type="hidden" name="opportunityId" value="<%= opportunity.id %>">
                                                        <button type="submit" class="btn btn-sm btn-outline-warning">
                                                            <i class="fas fa-star"></i> Save
                                                        </button>
                                                    </form>
                                                </td>
                                            </tr>
                                        <% }); %>
                                    </tbody>
                                </table>
                            </div>
                        <% } else { %>
                            <div class="text-center py-4">
                                <i class="fas fa-search fa-3x text-muted mb-3"></i>
                                <h5 class="text-muted">No opportunities found</h5>
                                <p class="text-muted">Try adjusting your search criteria</p>
                                <a href="/csrrepresentative/search-requests" class="btn btn-primary">
                                    <i class="fas fa-search"></i> New Search
                                </a>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <%- include('../partials/footer') %>
</body>
</html>
